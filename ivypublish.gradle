['ivy-2.2.0.jar', 'jsch-0.1.51.jar'].each {
    org.apache.tools.ant.Project.class.classLoader.addURL( file("${System.env.'GRADLE_HOME'}/lib/plugins/$it").toURI().toURL() )
}

ant.importBuild 'build.xml'
